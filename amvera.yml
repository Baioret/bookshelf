meta:
  environment: jvm
  toolchain:
    name: maven
    version: 17
build:
  artifacts:
    target/*.jar: /
  args: 'spring-boot:repackage '
run:
  jarName: bookshelf/target/bookshelf-1.jar
  persistenceMount: /data
  containerPort: 8080
